<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.dc.ccpt.modules.contractmanagement.contractStatistics.mapper.contractStatisticsMapper">
    
	<sql id="contractStatisticsColumns">
		a.id AS "id",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.del_flag AS "delFlag",
		a.remarks AS "remarks",
		
		a.conrtract_id,
		pc.contract_num AS "contract.num",
		pc.contract_name AS "contract.name",
		spc.subpro_contract_num AS "subProContract.num",
		spc.subpro_contract_name AS "subProContract.name"
		
	</sql>
	
	<sql id="contractStatisticsJoins">
		LEFT JOIN ccpt.program_contract pc ON pc.id = a.contract_id
		LEFT JOIN ccpt.sub_pro_contract spc ON spc.id = a.contract_id
	</sql>
	
	<select id="get" resultType="contractStatistics" >
		SELECT 
			<include refid="contractStatisticsColumns"/>
		FROM contract_statistics a
			<include refid="contractStatisticsJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="contractStatistics" parameterType="List">
		SELECT 
			<include refid="contractStatisticsColumns"/>
		FROM contract_statistics a
		<include refid="contractStatisticsJoins"/>
		<where>
			${dataScope}
<!-- 			<if test="contractStatisticsName != null and contractStatisticsName != ''"> -->
<!-- 				AND a.subpro_contract_name LIKE  -->
<!-- 					<if test="dbName == 'oracle'">'%'||#{contractStatisticsName}||'%'</if> -->
<!-- 					<if test="dbName == 'mysql'">concat('%',#{contractStatisticsName},'%')</if> -->
<!-- 			</if> -->
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="contractStatistics" >
		SELECT 
			<include refid="contractStatisticsColumns"/>
		FROM contract_statistics a
		<include refid="contractStatisticsJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			${dataScope}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO contract_statistics(
			id,
			create_by,
			create_date,
			update_by,
			update_date,
			del_flag,
			remarks
		) VALUES (
			#{id},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{delFlag},
			#{remarks}
		)
	</insert>
	
	<update id="update">
		UPDATE contract_statistics SET 	
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<!--物理删除-->
	<update id="delete">
		DELETE FROM contract_statistics
		WHERE id = #{id}
	</update>
	
	<!--逻辑删除-->
	<update id="deleteByLogic">
		UPDATE contract_statistics SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
	<!-- 根据实体名称和字段名称和字段值获取唯一记录 -->
	<select id="findUniqueByProperty" resultType="proContract" statementType="STATEMENT">
		select * FROM contract_statistics  where ${propertyName} = '${value}'
	</select>
	
</mapper>